[project]
name = "face-recognition"
version = "0.1.0"
description = ""
authors = [
    {name = "Michał Kwak",email = "michal.kwak2806@gmail.com"}
]
readme = "README.md"
requires-python = "^3.12"
dependencies = [
    "numpy (>=2.5.1,<3.0.0)",
    "scikit-learn (>=1.9.0,<2.0.0)",
    "pytest (>=9.1.1,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "coverage (>=7.15.2,<8.0.0)",
    "pylint (>=4.0.6,<5.0.0)"
]
